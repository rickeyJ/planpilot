%nav.navbar.navbar-default{role: "navigation"}
  .container
    .navbar-header
      %button.navbar-toggle{type: "button", "data-toggle" => "collapse", "data-target" => "#bs-example-navbar-collapse-1"}
        %span.sr-only Toggle navigation
        %span.icon-bar
        %span.icon-bar
        %span.icon-bar
      %a.navbar-brand{href: "/"} 
        = image_tag 'hpforyou_logo.png', class: 'img-responsive'

    .collapse.navbar-collapse{id: "bs-example-navbar-collapse-1"}
      %ul.nav.navbar-nav.navbar-right
        // Let's put the first four entries outside the dropdown
        - @navbar_entries[0..2].each do |entry|
          %li
            = link_to entry[:title], entry[:url]
        - if current_user
          %li
            = link_to "PROFILE", profile_path
          %li.bluetext
            = link_to "SIGN OUT (#{current_user.email[0..8]}...)", destroy_user_session_path, :method => :delete
        - else
          %li.bluetext
            %b
              = link_to('Login', new_user_session_path) + " | "

          %li.orangetext
            %b
              = link_to 'Sign Up'
          %li{style: 'margin-left: 20px; font-size: 0.9em; line-height: 1.7em;'}
            En Espa&#241;ol
